using System;
using System.Collections.Generic;
using System.Text;

namespace MethodSubmission
{
    public class Math
    {
        public int Add(int inputNumber1, int inputNumber2) // Add method will take in inputNumber from user
        {
            int addition = (inputNumber1 + inputNumber2);
            return addition;
        }
    }
}
